# An environment to develop, build, test, and document nbgitpuller-link
name: link
channels:
  - conda-forge
dependencies:
  - python =3
  - pip
  - click
  - validators
  - make
  - black
  - flake8
  - isort >=5
  - pytest
  - pytest-cov
  - pytest-datadir
  - coveralls
  - pip:
    - zest.releaser
